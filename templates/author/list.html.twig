<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des auteurs</title>
</head>
<body>
    <h1>Liste des auteurs</h1>

    {% if authors is defined and authors|length > 0 %}
        <ul>
            {% for author in authors %}
                <li>
                    <img src="{{ author.picture }}" alt="{{ author.username }}" width="80">
                    <strong>{{ author.username|upper }}</strong> <br>
                    Email : {{ author.email }} <br>
                    Nombre de livres : {{ author.nb_books }} <br>
                </li>
                <hr>
            {% endfor %}
        </ul>

        <p><strong>Nombre d’auteurs :</strong> {{ authors|length }}</p>
        <p><strong>Total des livres :</strong>
            {{ authors|map(a => a.nb_books)|reduce((carry, v) => carry + v) }}
        </p>
    {% else %}
        <p>Aucun auteur trouvé.</p>
    {% endif %}
</body>
</html>
